# childToParentProp_react
A simple React app demonstrating how to pass data from a child to a parent using a callback function. Shows the concept of lifting state up by syncing child and parent state.
